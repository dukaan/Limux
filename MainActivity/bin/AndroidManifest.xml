<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="inf.uie.Limux">

    <uses-sdk android:minSdkVersion="17" android:targetSdkVersion="21"/>

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application android:allowBackup="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:name="inf.uie.Limux.beacon.BeaconScannerApp" android:theme="@android:style/Theme.Holo">
        <activity android:label="@string/app_name" android:name="inf.uie.Limux.activity.MainActivity">
        </activity>
        <activity android:label="@string/title_activity_bluetoothdapter" android:name="inf.uie.Limux.bluetooth.Bluetooth">
        </activity>
        <activity android:configChanges="orientation|screenSize" android:label="@string/app_name" android:name="inf.uie.Limux.beacon.ScanActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name="inf.uie.Limux.beacon.AppPreferenceActivity">
            <intent-filter>
                <action android:name=".beacon.AppPreferenceActivity"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity android:label="@string/app_name" android:name="inf.uie.Limux.beacon.FileHandlerActivity" android:parentActivityName="inf.uie.Limux.beacon.ScanActivity">

            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".beacon.ScanActivity"/>
        </activity>
        <activity android:label="@string/title_activity_main_screen" android:name="inf.uie.Limux.beacon.MainScreenActivity">
        </activity>

        <service android:enabled="true" android:exported="true" android:isolatedProcess="false" android:label="beacon" android:name="org.altbeacon.beacon.service.BeaconService">
        </service>
        <service android:enabled="true" android:name="org.altbeacon.beacon.BeaconIntentProcessor">
        </service>
        <receiver android:name="org.altbeacon.beacon.startup.StartupBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.ACTION_POWER_CONNECTED"/>
                <action android:name="android.intent.action.ACTION_POWER_DISCONNECTED"/>
            </intent-filter>
        </receiver>
    </application>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

</manifest>
